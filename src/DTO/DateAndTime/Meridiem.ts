export default class Meridiem {
    uppercase: string;
    lowercase: string;

    constructor(uppercase: string, lowercase: string) {
        this.uppercase = uppercase;
        this.lowercase = lowercase;
    }
}